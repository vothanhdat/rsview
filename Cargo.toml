[package]
name = "rsview"
version = "0.1.0"
edition = "2021"
description = "Proof-of-concept: mmap + lazy byte-range JSON viewer in Rust (ratatui)"

[dependencies]
memmap2 = "0.9"
ratatui = "0.29"

[profile.release]
opt-level = 3
lto = true
